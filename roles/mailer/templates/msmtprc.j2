# Valeurs par défaut pour tous les comptes.
defaults
auth           on
tls            on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile        ~/.msmtp.log

account        default
host           smtp.gmail.com
port           587
from           {{ secrets.smtp.username }}
user           {{ secrets.smtp.username }}
password       {{ secrets.smtp.password }} 

